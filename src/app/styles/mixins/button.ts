import { css } from "styled-components";

const button = css`
  margin: 0;
  padding: 0;
  border: none;
  background: inherit;
  cursor: pointer;

  &:focus {
    outline: none;
  }
`;

export default button;
