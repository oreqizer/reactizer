import { css } from "styled-components";

const input = css`
  margin: 0;
  padding: 0;
  border: none;
  appearance: none;
  -webkit-appearance: none;

  &:focus {
    outline: none;
  }
`;

export default input;
